{
  "scripts": {
    "serve": "live-server ./",
    "build-babel": "babel src/ind.jsx --out-file=script/app.js --presets=env,react --watch",
    "build": "webpack -w"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "live-server": "^1.2.2",
    "webpack": "^5.74.0"
  },
  "devDependencies": {
    "webpack-cli": "^4.10.0"
  }
}
