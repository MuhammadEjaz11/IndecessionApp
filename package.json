{
  "scripts":{
    "serve": "live-server public/",
    "build": "babel src/ind.jsx --out-file=script/app.js --presets=env,react --watch"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-react": "^6.24.1",
    "live-server": "^1.2.2"
  }
}
